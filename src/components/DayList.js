import React from 'react';

const DayList = (props) => {

  const {
    days, // array
    day, // string
    setDay // function
  } = props;
  
  return (
    <ul>

    </ul>
  );
};

export default DayList;